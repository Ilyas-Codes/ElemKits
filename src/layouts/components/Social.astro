---
import * as ReactIcons from "react-icons/io5/index";
const { source, className } = Astro.props;
const { twitter, email } = source;
const { IoLogoTwitter, IoMail } = ReactIcons;
---

<ul class={className}>
  {
    twitter && (
      <li>
        <a
          class="hover-transition hover:text-main-blue"
          aria-label="twitter"
          href={twitter}
          target="_blank"
          rel="noopener noreferrer nofollow"
        >
          <IoLogoTwitter size={20} />
        </a>
      </li>
    )
  }
  {
    email && (
      <li>
        <a
          class="hover-transition hover:text-main-blue"
          aria-label="email"
          href={`mailto:${email}`}
        >
          <IoMail size={20} />
        </a>
      </li>
    )
  }
</ul>
